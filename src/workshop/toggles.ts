/**
 * Workshop feature switches.
 * Flip these booleans live during the workshop.
 */

// false = keep classic in-app tab bar, true = use floating tab bar variant
export const USE_FLOATING_TAB_BAR = true;

// false = keep legacy StyleSheet components, true = swap to Restyle components
export const USE_RESTYLE_COMPONENTS = true;

// false = keep FlatList baseline, true = use FlashList masonry layout
export const USE_FLASHLIST_MASONRY = true;

// false = use legacy listing details layout, true = use redesigned modern listing details layout
export const USE_MODERN_LISTING_DETAILS = true;

// false = plain heart icon toggle, true = animated heart burst effect on save
export const USE_ANIMATED_HEART = true;

// Number of marketplace items to generate (10, 50, 100, 500, 1000, etc.)
export const LISTING_COUNT = 10;
